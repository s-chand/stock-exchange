{
  "name": "stock-exchange",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-http": "^3.0.0",
    "jshint": "^2.9.5",
    "mocha": "^5.0.4",
    "nodemon": "^1.17.2",
    "nyc": "^11.6.0",
    "sequelize-cli": "^4.0.0"
  },
  "dependencies": {
    "express": "^4.16.3",
    "pg": "^7.4.1",
    "pg-hstore": "^2.3.2",
    "sequelize": "^4.37.3"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migration": "sequelize db:migrate",
    "seed": "sequelize db:seed:all",
    "test": "NODE_ENV=test nyc --reporter=html --reporter=text mocha --reporter spec --recursive test/ --exit"
  },
  "jshintConfig": {
    "esversion": 6
  },
  "nyc": {
    "exclude": [
      "**/*.test.js",
      "config",
      "models"
    ]
  }
}
