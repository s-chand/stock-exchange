{
  "name": "stock-exchange",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Samuel Okoroafor <samuel.i.okoroafor@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "jshint": "^2.9.5"
  },
  "scripts": {
    "pretest": "docker-compose up -d && docker-compose exec exchange npm run migration ",
    "test": "docker-compose exec exchange npm test ",
    "posttest": "docker-compose down",
    "prestart": "docker-compose build",
    "start": "docker-compose up"
  }
}
